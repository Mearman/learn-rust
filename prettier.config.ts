import type { Config } from "prettier";

const config: Config = {
    useTabs: false,
    tabWidth: 4,
    singleQuote: false,
    trailingComma: "es5",
    printWidth: 80,
    semi: true,
    endOfLine: "lf",
};

export default config;
